% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/icc_difference.R
\name{icc_difference}
\alias{icc_difference}
\title{Absolute Difference Between Intra-Condition Correlations (ICC)}
\usage{
icc_difference(
  icc_result,
  cutoff = NULL,
  plot = FALSE,
  heatmap_colors = colorRampPalette(c("#fffd8d", "yellow", "red")),
  heatmap_breaks = seq(0, 2, by = 0.5),
  show_rownames = TRUE,
  show_colnames = TRUE,
  cluster_rows = TRUE,
  cluster_cols = TRUE,
  fontsize = 8
)
}
\arguments{
\item{icc_result}{List containing ICC results from icc_analysis()}

\item{cutoff}{Optional numeric threshold for filtering differences (NULL returns all)}

\item{plot}{Logical indicating whether to generate a heatmap (default: FALSE)}

\item{heatmap_colors}{Colors for heatmap gradient (default: yellow to red)}

\item{heatmap_breaks}{Break points for color bins (default: seq(0, 2, by=0.5))}

\item{show_rownames}{Logical indicating whether to show row names in heatmap (default: TRUE)}

\item{show_colnames}{Logical indicating whether to show column names in heatmap (default: TRUE)}

\item{cluster_rows}{Logical indicating whether to cluster rows (default: TRUE)}

\item{cluster_cols}{Logical indicating whether to cluster columns (default: TRUE)}

\item{fontsize}{Base fontsize for heatmap (default: 8)}
}
\value{
A list containing:
\itemize{
\item absolute_diff_matrix - Full matrix of absolute differences
\item difference_table - Data frame of gene pairs and their differences
\item heatmap - (Optional) Heatmap object when plot=TRUE
}
}
\description{
Computes absolute differences between gene-gene correlation matrices from two conditions,
returns results in a tidy format, and provides visualization options.
}
\examples{
\dontrun{
data <- matrix(rnorm(80), nrow=8, ncol=10)
colnames(data) <- paste0("Gene_", 1:10)
icc_result <- icc_analysis(data, condition_split = c(4, 8))

# Basic usage with gene names visible by default
diff_result <- icc_difference(icc_result, plot = TRUE)
}

}
