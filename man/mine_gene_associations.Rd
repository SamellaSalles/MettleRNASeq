% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mine_gene_associations.R
\name{mine_gene_associations}
\alias{mine_gene_associations}
\title{Mine Gene Associations with Rule-Based Algorithms and Visualizations}
\usage{
mine_gene_associations(
  data,
  condition_rows,
  method = c("apriori", "fpgrowth"),
  supp = 0.5,
  conf = 0.8,
  lift = NULL,
  minlen = 2,
  maxlen = 10,
  target_genes = NULL,
  search_type = c("both", "lhs", "rhs"),
  plot_type = c("scatter", "graph", "paracoord", "grouped"),
  interactive = TRUE,
  top_n = 100,
  measure = "support",
  shading = "lift",
  color = "#0073e6",
  lhs_max = 50,
  rhs_max = 50
)
}
\arguments{
\item{data}{A data frame or matrix of gene expression (samples in rows, genes in columns).}

\item{condition_rows}{A numeric vector specifying which rows (samples) to include in the analysis.}

\item{method}{The mining algorithm to use. Options are \code{"apriori"} (default) or \code{"fpgrowth"}.}

\item{supp}{Minimum support threshold (default: 0.5).}

\item{conf}{Minimum confidence threshold (default: 0.8).}

\item{lift}{Optional minimum lift threshold to filter rules (default: \code{NULL}, meaning no lift filtering).}

\item{minlen}{Minimum length of rules (default: 2).}

\item{maxlen}{Maximum length of rules (default: 10).}

\item{target_genes}{Optional character vector of gene names to focus on.}

\item{search_type}{Where to match \code{target_genes}: \code{"both"} (default), \code{"lhs"} (antecedent), or \code{"rhs"} (consequent).}

\item{plot_type}{The type of plot: \code{"scatter"}, \code{"graph"}, \code{"paracoord"}, or \code{"grouped"} (default: \code{"scatter"}).}

\item{interactive}{Logical. Use interactive visualizations if \code{TRUE} (default).}

\item{top_n}{The number of top rules to include in the plot (default: 100).}

\item{measure}{Plotting measure for \code{grouped} plots (\code{"support"}, \code{"confidence"}, \code{"lift"}, or \code{"order"}). Default is \code{"support"}.}

\item{shading}{Shading measure for \code{grouped} plots (default: \code{"lift"}).}

\item{color}{Base color for \code{grouped} plots (default: \code{"#0073e6"}).}

\item{lhs_max}{Maximum items to show on the left-hand side (LHS) in grouped plots (default: 50).}

\item{rhs_max}{Maximum items to show on the right-hand side (RHS) in grouped plots (default: 50).}
}
\value{
A list with the following elements:
\describe{
\item{transactions}{The \code{transactions} object used for rule mining.}
\item{rules}{The mined association rules (of class \code{rules}).}
\item{rules_df}{A data frame representation of the rules.}
\item{plot}{The generated plot object, if applicable.}
}
}
\description{
This function performs association rule mining on gene expression data using either the Apriori or FP-Growth algorithm.
It provides filtering options (support, confidence, lift, rule length) and multiple visualization styles.
}
\details{
The function automatically installs required packages if they are not already installed. The \code{lift} filter
is applied after computing the \code{lift} measure explicitly to ensure compatibility across methods.

For \code{"fpgrowth"}, rules are mined using the \code{fim4r()} function from the \code{arules} package.
}
\examples{
\dontrun{
data <- matrix(rnorm(80), nrow=8, ncol=10)
colnames(data) <- paste0("Gene_", 1:10)
result <- mine_gene_associations(data, 1:4)
result <- mine_gene_associations(data, 1:4, plot_type = "paracoord", top_n = 5)
result <- mine_gene_associations(data, 1:4, method = "fpgrowth", plot_type = "graph", interactive = FALSE, lift = 2)

#Focusing on genes of interest
result <- mine_gene_associations(data, 1:4, plot_type = "graph", interactive = TRUE, top_n = 10, target_genes = "Gene_1", search_type = "lhs")
}

}
