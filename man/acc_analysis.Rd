% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/acc_analysis.R
\name{acc_analysis}
\alias{acc_analysis}
\title{Across-Condition Correlations (ACC) Analysis}
\usage{
acc_analysis(
  data,
  condition_split,
  method = "spearman",
  plot = FALSE,
  color = c("red", "steelblue"),
  orientation = c("vertical", "horizontal"),
  show_legend = TRUE,
  rotate_labels = TRUE,
  title = "Across-Condition Correlations",
  xlab = "Gene",
  ylab = "Correlation"
)
}
\arguments{
\item{data}{A data frame or matrix containing gene expression data with rows as samples and columns as genes.}

\item{condition_split}{Numeric vector of length 2 specifying the row indices that separate conditions
(e.g., c(4, 8) means rows 1-4 are condition 1, rows 5-8 are condition 2).}

\item{method}{Correlation method to use ("pearson", "kendall", or "spearman"). Default is "spearman".}

\item{plot}{Logical indicating whether to generate a bar plot. Default is FALSE.}

\item{color}{Color for the plot elements. Specify either:
- Single color (e.g., "steelblue") for all bars
- Vector of two colors (e.g., c("red", "blue")) for negative/positive correlations
Default is c("red", "steelblue").}

\item{orientation}{Plot orientation: "vertical" (default) or "horizontal" (uses coord_flip()).}

\item{show_legend}{Logical indicating whether to show the legend when using two colors. Default is TRUE.}

\item{rotate_labels}{Logical indicating whether to rotate x-axis labels (vertical plots only). Default is TRUE.}

\item{title}{Plot title. Default is "Across-Condition Correlations".}

\item{xlab}{X-axis label. Default is "Gene" (becomes y-label in horizontal orientation).}

\item{ylab}{Y-axis label. Default is "Correlation" (becomes x-label in horizontal orientation).}
}
\value{
A data frame containing genes and their correlation values, and optionally a ggplot object.
}
\description{
Calculates correlations between same genes across two different conditions and optionally plots the results.
Note: This function only works for comparing exactly 2 conditions at a time.
}
\examples{
\dontrun{
# Vertical bars with rotated labels (default)
result <- acc_analysis(data, condition_split = c(4, 8), plot = TRUE)

# Horizontal bars (automatically swaps axes)
result <- acc_analysis(data, condition_split = c(4, 8), plot = TRUE, 
                     orientation = "horizontal")

# Vertical bars without label rotation
result <- acc_analysis(data, condition_split = c(4, 8), plot = TRUE,
                     rotate_labels = FALSE)
}

}
